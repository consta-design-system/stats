import { StatsExampleContent } from './examples/StatsExampleContent';
import { StatsExampleRateDown } from './examples/StatsExampleRate';
import { StatsExampleSuccess } from './examples/StatsExampleStatus';

```tsx
import { Stats } from '@consta/stats/Stats';
```

Этот компонент нужен, чтобы показывать важные числа — на графиках, схемах, картах или сами по себе.
Например, количество филиалов или процент прибыли.

Число может быть очень заметным, к нему можно добавить комментарии.

<StatsExampleContent />
<StatsExampleRateDown />
<StatsExampleSuccess />
